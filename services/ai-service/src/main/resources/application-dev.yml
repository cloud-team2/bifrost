openai:
  api-key: ${OPENAI_API_KEY:}
  model: ${OPENAI_MODEL:gpt-4o-mini}
  temperature: 0.3

# core-service의 위치 (도구 호출 시 사용)
core-service:
  url: ${CORE_SERVICE_URL:http://localhost:8080}

logging:
  level:
    root: INFO
    com.platform: DEBUG
    dev.langchain4j: INFO
