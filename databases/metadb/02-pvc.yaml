apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metadb-pvc
  namespace: metadb
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 20Gi
