---
apiVersion: v1
kind: Secret
metadata:
  name: tenant-postgres-credentials
  namespace: tenantdb
type: Opaque
stringData:
  POSTGRES_USER: debezium
  POSTGRES_PASSWORD: debezium
  POSTGRES_DB: testdb
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tenant-postgres-pvc
  namespace: tenantdb
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-postgres
  namespace: tenantdb
  labels:
    app: tenant-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tenant-postgres
  template:
    metadata:
      labels:
        app: tenant-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          args:
            - postgres
            - -c
            - config_file=/etc/postgresql/postgresql.conf
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: tenant-postgres-credentials
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: postgres
            - name: config
              mountPath: /etc/postgresql/postgresql.conf
              subPath: postgresql.conf
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "debezium", "-d", "testdb"]
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: tenant-postgres-pvc
        - name: config
          configMap:
            name: tenant-postgres-config
---
apiVersion: v1
kind: Service
metadata:
  name: tenant-postgres-service
  namespace: tenantdb
spec:
  selector:
    app: tenant-postgres
  ports:
    - port: 5432
      targetPort: 5432
  # 내부 통신만 사용(커넥터는 cluster DNS 직통, 로컬 백엔드는 port-forward). 외부 LB 미사용.
  type: ClusterIP
