apiVersion: kafka.strimzi.io/v1
kind: KafkaConnect
metadata:
  name: platform-connect
  namespace: platform-kafka
  annotations:
    # CRD 기반 connector 관리 활성화
    strimzi.io/use-connector-resources: "true"
spec:
  version: 4.2.0
  replicas: 2
  bootstrapServers: platform-kafka-kafka-bootstrap:9092

  # 커스텀 이미지 (Debezium plugins 포함) — Docker Hub
  image: hwnnn/bifrost-kafka-connect:latest

  # Strimzi 1.0.0: 필수 필드가 spec 최상위로 이동
  groupId: platform-connect
  offsetStorageTopic: platform-connect-offsets
  configStorageTopic: platform-connect-configs
  statusStorageTopic: platform-connect-status

  config:
    offset.storage.replication.factor: 3
    config.storage.replication.factor: 3
    status.storage.replication.factor: 3
    key.converter: org.apache.kafka.connect.json.JsonConverter
    value.converter: org.apache.kafka.connect.json.JsonConverter
    key.converter.schemas.enable: false
    value.converter.schemas.enable: false
    # CDC 출력 토픽 자동 생성 (auto.create.topics.enable=false 환경 대응)
    topic.creation.enable: "true"
    topic.creation.default.replication.factor: "3"
    topic.creation.default.partitions: "3"
    topic.creation.default.cleanup.policy: delete
    topic.creation.default.retention.ms: "604800000"
  
  resources:
    requests:
      memory: 1Gi
      cpu: 500m
    limits:
      memory: 2Gi
      cpu: 2
