---
apiVersion: v1
kind: Secret
metadata:
  name: tenant-mariadb-credentials
  namespace: tenantdb
type: Opaque
stringData:
  MARIADB_ROOT_PASSWORD: root
  MARIADB_USER: debezium
  MARIADB_PASSWORD: debezium
  MARIADB_DATABASE: testdb
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tenant-mariadb-pvc
  namespace: tenantdb
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-mariadb
  namespace: tenantdb
  labels:
    app: tenant-mariadb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tenant-mariadb
  template:
    metadata:
      labels:
        app: tenant-mariadb
    spec:
      containers:
        - name: mariadb
          image: mariadb:10.11
          ports:
            - containerPort: 3306
          envFrom:
            - secretRef:
                name: tenant-mariadb-credentials
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
            - name: config
              mountPath: /etc/mysql/conf.d/cdc.cnf
              subPath: my.cnf
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command:
                - bash
                - -c
                - "mariadb-admin ping -u root -p${MARIADB_ROOT_PASSWORD} --silent"
            initialDelaySeconds: 15
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: tenant-mariadb-pvc
        - name: config
          configMap:
            name: tenant-mariadb-config
---
apiVersion: v1
kind: Service
metadata:
  name: tenant-mariadb-service
  namespace: tenantdb
spec:
  selector:
    app: tenant-mariadb
  ports:
    - port: 3306
      targetPort: 3306
  # 내부 통신만 사용(커넥터는 cluster DNS 직통, 로컬 백엔드는 port-forward). 외부 LB 미사용.
  type: ClusterIP
