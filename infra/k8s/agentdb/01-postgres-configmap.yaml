# agentdb 최초 기동 시 pgvector 확장 생성 (Knowledge Vector Store용).
# pgvector/pgvector 이미지는 vector 확장을 포함하므로 CREATE EXTENSION만 하면 된다.
apiVersion: v1
kind: ConfigMap
metadata:
  name: agentdb-init
  namespace: agentdb
data:
  01-pgvector.sql: |
    CREATE EXTENSION IF NOT EXISTS vector;
