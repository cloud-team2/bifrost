# FastAPI ai-service 전용 DB (Agent Run Store + Knowledge Vector Store).
# 설계 §9.1은 metadb 인스턴스 co-locate를 v1로 두었으나, #119로 클러스터 용량이
# 확보되어 서비스 경계를 깔끔히 하기 위해 별도 pgvector 인스턴스로 분리한다.
apiVersion: v1
kind: Namespace
metadata:
  name: agentdb
  labels:
    app.kubernetes.io/managed-by: kubectl
