apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      # DB 스키마 마이그레이션 — 앱 컨테이너 기동 전에 alembic 적용 (idempotent, 이미 head면 no-op)
      initContainers:
        - name: migrate
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["alembic", "upgrade", "head"]
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8082
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            - name: AI_LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ai-llm-secret
                  key: api-key
                  optional: true
            # (#646) /internal/ops 게이트 토큰. ops-backend와 동일 시크릿·키를 참조(같은 bifrost-system ns).
            # 키가 없으면 미설정 → ai-service가 헤더 미동봉, ops-backend 게이트도 비활성(무중단).
            - name: AI_INTERNAL_OPS_TOKEN
              valueFrom:
                secretKeyRef:
                  name: operations-backend-secrets
                  key: INTERNAL_OPS_TOKEN
                  optional: true
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 15
          readinessProbe:
            httpGet:
              path: /api/v1/ready
              port: 8082
            initialDelaySeconds: 10
